xml = Neos.Fusion:Case {
    feed {
        condition = ${!site.context.inBackend && q(documentNode).is('[instanceof NeosRulez.Blog:Document.Feed]')}
        type = 'NeosRulez.Blog:Component.Feed'
    }
}
xml.@if.cond = ${!site.context.inBackend}

prototype(NeosRulez.Blog:Document.Feed) < prototype(Neos.NodeTypes:Page) {
    body = afx`

    `
    body.@if.cond = ${site.context.inBackend}
}
